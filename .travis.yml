dist: "xenial"
language: python
python:
  - "2.7"
  - "3.6"
  - "3.7"
before_install:
    - pip install poetry
install:
    - poetry install
script:
    - poetry run nosetests --with-coverage --cover-package=pysnow --cover-erase
after_success:
    - coveralls
