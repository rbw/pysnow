dist: "xenial"
language: python
python:
  - "2.7"
  - "3.6"
  - "3.7"
before_install:
    - pip install poetry
install:
    - pip install $(poetry run pip freeze)
script:
    - nosetests --with-coverage --cover-package=pysnow --cover-erase
after_success:
    - coveralls
